<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.company.studytool">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/newicone_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Studytool">

        <activity
            android:name=".View.MyToDoListActivity"
            android:launchMode="singleTop" />

        <activity
            android:name=".View.AddEditNodeActivity"
            android:parentActivityName=".View.MyToDoListActivity" />

        <activity android:name=".View.privateMessageActivity" />


        <activity android:name=".View.VideoFullScreenActivity" />


        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity android:name=".View.FacebookActivity" />
        <activity android:name=".View.StudentListActivity" />
        <activity android:name=".View.ForgotPasswordActivity" />
        <activity android:name=".View.TruthTableActivity" />

        <activity android:name=".View.DigitalVideoActivity" />

        <activity android:name=".View.SelectedCourseActivity" />

        <activity android:name=".View.CoursesActivity" />
        <activity android:name=".View.AddProductActivity"/>

        <activity android:name=".View.MainActivity">


            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity android:name=".View.PracticeActivity" />

        <activity android:name=".View.ShowScoreActivity" />

        <activity android:name=".View.DigitalBookActivity" />

        <activity android:name=".View.loginActivity" />

        <activity android:name=".View.MenuActivity" />

		<activity android:name=".View.RegisterActivity" />
		<activity android:name=".View.StartQuizActivity" />
		<activity android:name=".View.QuizActivity" />

        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider181738426848830"
            android:exported="true" />
    </application>
		</manifest> 
